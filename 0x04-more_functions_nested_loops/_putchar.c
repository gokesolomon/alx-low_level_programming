#include "main.h"
#include <unistd.h>
/**
 * _putchar.c - writes the character c to stdout
 * @c: character to print
 *
 * Return on success 1.
 * on error -1 is retiurned, and error is set appropriately.
 */
int _putchar.c(char c)
{
	return (write(1, & c, 1));
}
